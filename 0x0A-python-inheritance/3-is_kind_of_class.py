#!/usr/bin/python3
""" if object is an instance,or if object is instance of a class"""


def is_kind_of_class(obj, a_class):
    """the specified class ; otherwise False"""
    return isinstance(obj, a_class)
